import { environmentConfig } from './config/environment';

console.log('🚀 Report Builder starting...');
console.log(`Environment: ${environmentConfig.environment}`);
console.log(`AWS Region: ${environmentConfig.awsRegion}`);

// Main application logic will be added here
export default {}; 