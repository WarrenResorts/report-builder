import { config } from './config/environment';

console.log('🚀 Report Builder starting...');
console.log(`Environment: ${config.environment}`);
console.log(`AWS Region: ${config.awsRegion}`);

// Main application logic will be added here
export default {}; 